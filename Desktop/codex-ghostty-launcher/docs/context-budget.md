# Context Budget

- Stable policy lives in `AGENTS.md`.
- Mutable task state lives in the shared artifact.
- The runtime wrapper should only carry project facts and the current role.
- Do not repeat the output contract outside the shared artifact.
